import React, { Fragment } from 'react';
import Sections from '../sections/Sections';

const Layout = () => {
  return (
    <Fragment>
      <main>
        <Sections />
      </main>
    </Fragment>
  );
};

export default Layout;
